{
  "framework": "vite",
  "buildCommand": "pnpm --filter @workspace/rsvp-site run build",
  "outputDirectory": "artifacts/rsvp-site/dist",
  "installCommand": "pnpm install",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
